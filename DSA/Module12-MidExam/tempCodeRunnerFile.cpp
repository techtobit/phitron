        auto it = find(address.begin(), address.end(), commands);
        if (*it == commands)
        {
            cout << *it << endl;
        }
        else
            cout << "Not Available" << endl;